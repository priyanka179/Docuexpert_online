import { EasingFunc, RenderState } from './types';
export declare class Transition {
    validate: ((fromtState: string, toState: string) => boolean);
    steps: RenderState<any>[];
    duration: number;
    easing: EasingFunc;
    constructor(validate: ((fromtState: string, toState: string) => boolean));
}
