export declare abstract class ColorProviderService {
    abstract nextColor(): string;
    abstract nextColorByName(name: string): string;
}
export declare class DefaultColorProviderService extends ColorProviderService {
    private classes2colors;
    constructor();
    nextColor(): string;
    nextColorByName(name: string): string;
}
